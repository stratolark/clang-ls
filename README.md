# C Learning Project

This project is a learning exercise in C programming.

To build and run the project, follow these steps:

3. Build the project: `gcc -std=c17 -O2 -Wall -Wextra -Wpedantic -Wconversion -Wshadow -o lsbtw ls.c`
4. Run the executable: `./lsbtw`
5. Clean up: `rm -f lsbtw`
